function fullName(first, last) {
  return `${first} ${last}`;
}

const name = fullName('Jean', 'Martin');
console.log(name);

const identity = {
  first: 'Jean',
  last: 'Dupont',
  email: 'jean@gmail.com',
};
